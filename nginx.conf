events {}

http {

    resolver 127.0.0.11 valid=5s;

    upstream backend {
        zone backend 64k;
        least_conn;

        server app:8080 resolve;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://backend;
        }
    }
}